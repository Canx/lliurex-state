name: Update Package Information

on:
  schedule:
    # Ejecutar todos los domingos a las 02:00 UTC
    - cron: '0 2 * * 0'
  workflow_dispatch: # Permite ejecutar manualmente el workflow

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  update-packages:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0  # Fetch all history for proper merging

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Fetch package information
      run: |
        python fetch_packages.py

    - name: Commit and push package changes
      uses: stefanzweifel/git-auto-commit-action@v5
      with:
        commit_message: "📦 Update package information (weekly)"
        commit_user_name: "github-actions[bot]"
        commit_user_email: "github-actions[bot]@users.noreply.github.com"
        commit_author: "github-actions[bot] <github-actions[bot]@users.noreply.github.com>"
        file_pattern: "packages_state.json changes_timestamps.json packages_*.json"
        pull_options: '--rebase --autostash'
        push_options: '--force-with-lease'

    - name: Upload Pages artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: '.'

  deploy:
    needs: update-packages
    runs-on: ubuntu-latest

    permissions:
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
